customCommands:
  - key: "<c-a>"
    context: "files"
    command: 'bash -c ''msg=$(generate-commit-message | sed "s/^[0-9]*\\. //" | fzf --prompt="Commit> " --height=50% --border --reverse) && git commit -m "$msg"'''
    description: "Generate AI commit message"
    output: terminal
